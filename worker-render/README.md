# worker-render (Node)

Phase 1 Remotion UGC 9:16 composition.

```bash
npm install
node scripts/render.mjs path/to/manifest.json path/to/output.mp4
```

Go worker-pipeline invokes this when `RENDER_MOCK` is not set.

See [../../docs/RENDER.md](../../docs/RENDER.md).
